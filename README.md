# model_eval_tools
Python tools used for handelling Met Office model output for the purposes of model evaluation using scintillometry obervations in London

install with pip install -e D:/Documents/model_eval_tools
